version: "3.9"
services:
  ykdev:
    build:
      context: ./
      dockerfile: Dockerfile
    privileged: true
    volumes:
      - "/dev:/dev"
    environment: 
      - URL=${DEFGUARD_URL}
      - WORKER_ID=${WORKER_ID}
      - DEFGUARD_TOKEN=${DEFGUARD_TOKEN}

